<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_height="@dimen/contact_cell_size"
	android:layout_width="@dimen/contact_cell_size"
	android:layout_margin="8dp"
	android:orientation="horizontal"
	tools:context=".activity.ContactsFragment"
	>

	<!-- Height and width will be set programmatically. -->
	<me.lazerka.mf.android.adapter.BadgeView
		android:id="@+id/userpic"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:contentDescription="@string/userpic_alt"
		android:layout_gravity="center_vertical"/>

	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="match_parent"
	    android:layout_gravity="bottom"
		>
		<TextView
			android:id="@+id/name"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:textSize="16sp"
			android:fontFamily="Roboto Regular"
			android:textColor="@android:color/primary_text_light"
			android:gravity="center_horizontal"
			android:singleLine="true"
			/>
	</LinearLayout>

	<!-- Hidden by width -->
<!--
	<ImageButton
		android:id="@+id/remove"
		android:layout_width="0dp"
		android:layout_height="0dp"
		android:visibility="gone"
		android:src="@android:drawable/ic_menu_close_clear_cancel"
		android:layout_gravity="center_vertical"
		android:background="@color/transparent"
		android:contentDescription="@string/delete_alt"
		/>
-->
</LinearLayout>
